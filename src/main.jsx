import React from "react";
import ReactDOM from "react-dom/client";
import PolicyLibraryApp from "../policy_library.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PolicyLibraryApp />
  </React.StrictMode>
);
